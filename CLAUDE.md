# CLAUDE.md

このファイルは、本リポジトリで作業する Claude Code（および開発者）が **常に守るべきルール** を定めます。
特定の作業でのみ必要になる詳細ルールは `.claude/rules/` 以下に分割し、本ファイル末尾の一覧から参照します。

---

## 全作業に適用される共通ルール

### 言語

- ルール・ドキュメント・ログはすべて **日本語** で記述する。
- コード中の識別子など、日本語化がそぐわないものは英語のままでよい。

### ドキュメントの書き出し

- ドキュメントやルールを作成・更新するときは、**タイムアウトを防ぐために段階的に書き出し、こまめにコミットする**。
  - 1 つの大きなファイルを一度に書き切ろうとせず、節ごと・ファイルごとに分けて書き、その都度コミットする。
  - コミットメッセージは変更内容が分かる簡潔な日本語にする。

### 作業ログ（ユーザからの指摘対応）

- **ユーザからの指摘を元に実施した作業の内容は `logs/claude-日時.log` に記録する。**
  - ファイル名の日時は作業開始時刻を `YYYYMMDD-HHMMSS` 形式で用いる（例: `logs/claude-20260718-102058.log`）。
  - 記録内容: 指摘の要約 / 実施した作業 / 変更したファイル / 補足事項。

---

## 詳細ルール一覧（`.claude/rules/`）

必要になったときに以下を参照する。各ファイルの冒頭に適用対象と要点を記載している。

| ファイル | 内容 | 参照するタイミング |
| :-- | :-- | :-- |
| [`.claude/rules/skill-creation.md`](.claude/rules/skill-creation.md) | スキル（Skills）作成のルールと要点 | スキルを新規作成・改定するとき |
| [`.claude/rules/subagent-creation.md`](.claude/rules/subagent-creation.md) | サブエージェント（sub-agents）作成のルールと要点 | サブエージェントを新規作成・改定するとき |
| [`.claude/rules/skill-agent-lifecycle.md`](.claude/rules/skill-agent-lifecycle.md) | スキル・エージェントの履歴記録と廃止時の取り扱い | スキル/エージェントを作成・改定・廃止するとき |

---

## プロジェクト概要

プログラムの処理構造を **3 つの視点**（IO 分析 / 依存関係分析 / 画面関係分析）で可視化するツール。
利用技術は **Neo4j**（グラフ DB）と **Next.js**。

- 本プロジェクトの責務は「**Neo4j にどの構造のデータを登録するか**」の定義と、その構造を可視化するところまで。
- ソースを解析して Neo4j へ登録するシステムは **本プロジェクトの範囲外**。
- グラフ構造の一次定義は [`docs/graph-model.md`](docs/graph-model.md)。

## ディレクトリ構成

| パス | 用途 |
| :-- | :-- |
| `CLAUDE.md` | 全作業共通のルールと詳細ルール一覧（本ファイル） |
| `graph-model/` | Neo4j に登録するデータ構造の TypeScript 型定義（中核） |
| `neo4j/` | スキーマ（制約・索引）・サンプルデータ・compose |
| `docs/` | 設計ドキュメント（グラフモデル定義書など） |
| `src/` | Next.js 可視化アプリ（3 モード） |
| `.claude/rules/` | 必要時に参照する詳細ルール |
| `.claude/logs/` | スキル・エージェントの作成／改定履歴（`skills-日時.log`） |
| `.claude/deprecated/` | 廃止したスキル・エージェントの退避先（削除せず移動する） |
| `logs/` | ユーザ指摘対応など作業内容の記録（`claude-日時.log`） |
